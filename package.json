{
  "name": "cypress-ncatestify-plugin",
  "version": "1.0.2",
  "description": "NCA TESTIFY commands and tests",
  "main": "src",
  "types": "src",
  "files": [
    "**/*.js",
    "index.d.ts"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --skipLibCheck",
    "release": "npm run build && npm version && node scripts/release.js && npm publish dist"
  },
  "keywords": [],
  "author": "Roland Golla",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.11.0",
    "typescript": "^4.8.4"
  }
}
